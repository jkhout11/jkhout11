// The HomeScreen class displays the home screen and prompts the user for their choice. It returns the user's choice back to the AppController.
package org.example;

public class HomeScreen {
    private AppController appController;

    public HomeScreen(AppController appController) {
        this.appController = appController;
    }

    public void displayHomeScreen() {
        // Display home screen UI and options
    }

    // Other methods to handle user input and interact with the AppController
}